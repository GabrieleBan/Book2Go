export default class User {
    constructor({ name, surname, email, number }) {
        this.name = name;
        this.surname = surname;
        this.email = email;
        this.number = number;
    }
}